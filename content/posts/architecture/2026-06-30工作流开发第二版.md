---
title: "工作流开发第二版"
date: "2026-06-30"
tags: ["架构"]
ShowToc: false
TocOpen: false
---
审批的接口都收拢了 搞了一张主表 但是由于状态只是某些审批中的状态 审批外的状态需要联合业务表做  
还是有老问题 业务操作和审批操作切换 切换还得手动恢复流程 驳回操作写死了 流程并未结束 但是要做到流程外 所有使用审批中的状态(拒绝)来协同外部操作 很少用条件网关  
不支持监听节点结果回写主流程  
写死了拒绝就是待修改  
会签或签 指定assigneeList由框架处理
TaskListener 的 create 事件，并不是你能干预会签任务创建过程的“最早”时机。当 create 事件被触发时，会签需要多少个任务、分别分配给谁，这些关键信息已经被引擎计算完毕  
需要在ExecutionListener做监听把所有候选人添加进去 然后框架会触发多个任务  
但是现在做法只是在解析xml的时候做好 比如配置了role1 role2但是每个角色对应了很多人 应该去解析到人去生成任务 结果没有 只是2个任务每个角色一个任务  
```
<bpmn:multiInstanceLoopCharacteristics 
    camunda:collection="${assigneeList}" <!-- 指定集合变量 -->
    camunda:elementVariable="assignee"> <!-- 当前迭代的元素 -->
    ...
</bpmn:multiInstanceLoopCharacteristics>
```
角色不支持  